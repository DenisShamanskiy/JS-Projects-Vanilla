import "../styles/index.css";
import "../images/hex-random.png";

/*const projects = document.querySelector(".projects");

const project = [
  {
    id: 1,
    title: "HexRandom",
    href: "./hexRandom.html",
    src: "./images/hex-random.png",
  },
];

window.addEventListener("DOMContentLoaded", function () {
  projectList(project);
});

function projectList(pr) {
  let prod = pr.map(function (item) {
    console.log(item);

    return `<li class="projects__item">
                <a  href=${item.href}>
                    <img class="projects__item-link" src={require('${item.src})} title=${item.title}>
                    <span class="projects__item-link-span">${item.title}</span>
                </a>
            </li>`;
  });

  prod = prod.join("");
  console.log(prod);

  projects.innerHTML = prod;
}
*/
